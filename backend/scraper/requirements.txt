# Python deps for the backend scraper / synthesis pipeline.
# Pinned to known-good versions to avoid surprise breakage in the
# GitHub Action.

feedparser==6.0.11      # RSS/Atom parsing
requests==2.32.3        # HTTP client
python-dateutil==2.9.0  # robust date parsing across RSS formats
slugify==0.0.1          # stable URL-safe slugs for forecast IDs
unidecode==1.3.8        # transliteration for slug generation
ftfy==6.3.1             # "fixes text for you" - repairs UTF-8/cp1252 mojibake
anthropic==0.40.0       # Claude API client for classification + synthesis
