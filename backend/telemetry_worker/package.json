{
  "name": "ecm-telemetry-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ECM Forecasting Alerts telemetry (anonymous install pings + aggregated stats).",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.0"
  }
}
